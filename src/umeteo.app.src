%% vim: et ts=4 sw=4 ft=erlang
{application, umeteo, [
    {description, "UMB REST API Server"},
    {vsn, git},
    {registered, []},
    {applications, [kernel, stdlib, cowboy, sl]},
    {mod, {umeteo_app, []}}]}.
